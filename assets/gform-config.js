/* =======================================================
   口袋營養獅 — Google 表單嵌入設定
   聯絡頁的「留言給我們」直接嵌入你的 Google 表單，
   回覆會自動進到你建立表單時登入的 Google 帳號（建議用
   codaredietitian@gmail.com 建立），並可另外開啟「取得新回
   覆時通知我」功能，讓每次留言都寄一封通知信到 Gmail。

   設定步驟（約 3 分鐘）：
   1. 到 https://forms.google.com 用 codaredietitian@gmail.com
      登入並建立新表單，建議欄位：姓名、聯絡電話、Email、
      想諮詢的內容（跟原本表單一致即可）
   2. 表單編輯畫面右上角「傳送」（紙飛機圖示）
   3. 彈出視窗切到「嵌入」<> 分頁
   4. 會看到一段 <iframe src="https://docs.google.com/forms/d/e/xxxx/viewform?embedded=true" ...>
      只需要複製 src="" 裡面那一長串網址
   5. 貼到下面雙引號中間即可，存檔就會自動生效，不用改其他檔案
   6. 想收到留言通知信：回到表單「回覆」分頁 → 右上角「⋮」
      → 開啟「取得新回覆的電子郵件通知」
   ======================================================= */

const GOOGLE_FORM_EMBED_URL = "";
